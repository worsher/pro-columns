# 贡献指南

感谢您考虑为 Pro-Columns 做出贡献！

## 开发流程

### 1. Fork 和克隆仓库

```bash
# Fork 仓库到您的账户
# 然后克隆到本地
git clone https://github.com/YOUR_USERNAME/pro-columns.git
cd pro-columns
```

### 2. 安装依赖

```bash
pnpm install
```

### 3. 创建新分支

```bash
git checkout -b feature/your-feature-name
# 或
git checkout -b fix/your-bug-fix
```

### 4. 开发

```bash
# 开发模式（监听构建）
pnpm dev

# 运行测试
pnpm test

# 运行测试 UI
pnpm test:ui

# 类型检查
pnpm run type-check

# 代码检查
pnpm run lint

# 代码格式化
pnpm run format
```

### 5. 提交更改

请遵循以下提交信息规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type:**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

**示例:**
```bash
git commit -m "feat(strategy): 添加 Filter 策略支持自动筛选"
git commit -m "fix(preset): 修复 idField 预设宽度配置问题"
git commit -m "docs(readme): 更新 API 文档"
```

### 6. 推送和创建 Pull Request

```bash
git push origin feature/your-feature-name
```

然后在 GitHub 上创建 Pull Request。

## 代码规范

### TypeScript

- 使用 TypeScript 编写代码
- 确保所有公共 API 都有类型定义
- 避免使用 `any`，除非必要

### 测试

- 所有新功能都必须包含测试用例
- 修复 Bug 时添加回归测试
- 测试覆盖率应保持在 80% 以上
- 使用 Vitest 编写测试

```typescript
import { describe, it, expect } from 'vitest'

describe('Your Feature', () => {
  it('should work correctly', () => {
    expect(yourFunction()).toBe(expected)
  })
})
```

### 文档

- 为新功能添加文档
- 更新 README.md（如果需要）
- 添加 JSDoc 注释说明函数用途

```typescript
/**
 * 策略描述
 * @param options - 配置项
 * @returns 策略函数
 */
export function MyStrategy(options: Options): StrategyItem {
  // ...
}
```

## Pull Request 检查清单

在提交 PR 之前，请确保：

- [ ] 代码通过 `pnpm run lint` 检查
- [ ] 代码通过 `pnpm run type-check` 类型检查
- [ ] 所有测试通过 `pnpm run test:run`
- [ ] 构建成功 `pnpm run build`
- [ ] 添加了必要的测试用例
- [ ] 更新了相关文档
- [ ] 提交信息遵循规范
- [ ] PR 描述清晰说明了改动内容

## 报告 Bug

如果发现 Bug，请创建 Issue 并包含以下信息：

1. **Bug 描述** - 简要描述问题
2. **重现步骤** - 如何重现该问题
3. **期望行为** - 应该发生什么
4. **实际行为** - 实际发生了什么
5. **环境信息** - Node 版本、包版本等
6. **代码示例** - 最小可复现代码（如果可能）

## 功能请求

功能请求也欢迎！请创建 Issue 并说明：

1. **功能描述** - 您想要什么功能
2. **使用场景** - 为什么需要这个功能
3. **建议方案** - （可选）您认为如何实现

## 开发提示

### 策略开发

创建新策略时：

1. 在 `strategy/` 目录创建策略文件
2. 使用 `createStrategy` 工具函数
3. 在 `strategy/index.ts` 中导出
4. 创建对应的测试文件
5. 更新文档

示例结构：
```
strategy/
├── MyStrategy.ts        # 策略实现
├── MyStrategy.test.ts   # 测试文件
└── index.ts            # 导出
```

### 预设开发

创建新预设时：

1. 在 `presets/index.ts` 中添加静态方法
2. 组合现有策略
3. 在测试文件中添加测试
4. 更新文档中的预设表格

### 运行特定测试

```bash
# 运行单个测试文件
pnpm test strategy/MyStrategy.test.ts

# 运行匹配的测试
pnpm test -- --grep "MyStrategy"
```

## 问题和帮助

如果遇到任何问题，欢迎：

- 创建 Issue
- 在 PR 中讨论
- 联系维护者 [@worsher](https://github.com/worsher)

感谢您的贡献！🎉
